#include <iostream>
using namespace std;

int Add(int a, int b);

int main()
{
    int a, b;
    cout << "a = ? "; cin >> a;
    cout << "b = ? "; cin >> b;

    cout << "a + b = " << Add(a, b) << endl;
}

int Add(int x, int y)
{
}
